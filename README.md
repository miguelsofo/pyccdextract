# pyccdextract
Event extraction tool for CCD and Skipper-CCD images.
Start with example_extract.py.

The input is a folder with fits images. All the images has to be preprocessed to substract the overscan (baseline).
The optput is an HDF5 file. 
